using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestRankingInfo : MonoBehaviour {

	// Use this for initialization
	void Start () {
        //StaticData.LevelID = 1001;
        //int myRank = RankingInfo.Instance.GetMyRank(StaticData.LevelID);
        //StaticData.GetRankInfo(0,10, myRank,false,()=>{
        //    List<PlayerInfoObject>  lowerList = RankingInfo.Instance.GetLowerInfos(5, StaticData.LevelID);
        //    foreach(PlayerInfoObject pio in lowerList)
        //    {
		//        LogManager.Log(pio.NickName , ":" ,pio.m_Rank);
        //    }
        //    List<PlayerInfoObject> higherList = RankingInfo.Instance.GetHigherInfos(4, StaticData.LevelID);
        //    foreach (PlayerInfoObject pio in higherList)
        //    {
		//        LogManager.Log(pio.NickName , ":" , pio.m_Rank);
        //    }
        //});


	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
